tr_root: ${path_prefix}/data/synthtreeseg/training/
val_root: # ${path_prefix}/data/synthtreeseg/validation/

raw_name: volumes/raw
gt_name: volumes/labels/neuron_ids
mask_name: volumes/labels/labels_mask
voxel_size: [20, 9, 9]
in_memory: true
enable_mask: true
labels_padding: null
